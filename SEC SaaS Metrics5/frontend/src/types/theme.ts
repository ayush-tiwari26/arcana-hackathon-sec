export type ThemeType = {
  backgroundColor: string;
  color: string;
  fontFamily: string;

  sidebar: {
    backgroundColor: string;
    color: string;

    hoverBackground: string;
    hoverColor: string;
    themeSwitcherBackground: string;
  };

  button: {
    backgroundColor: string;
    color: string;
  };

  table: {
    headerColor: string;
  };
};
